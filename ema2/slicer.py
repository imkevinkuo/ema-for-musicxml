from xml.dom import minidom
from ema2.emaexpression import EmaExpression
# partwise - also only works on .musicxml, not .mxl (compressed format)


def slice_score(score: minidom.Document, ema_exp: EmaExpression):
    return None
